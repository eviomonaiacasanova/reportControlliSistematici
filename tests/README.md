# Test browser

I test non richiedono server, dipendenze o connessione internet.

- Aprire `model-smoke.html` per verificare normalizzazione, clonazione, reset, check/uncheck e nomi degli export.
- Aprire `app-load-smoke.html` per verificare il caricamento dei moduli e l'inizializzazione dell'interfaccia.
- Aprire `pdf-export-smoke.html` per verificare il passaggio esplicito del modello e i nomi dei PDF esportati.
- Aprire `image-utils-smoke.html` per verificare conversione, ridimensionamento e inserimento delle immagini nel PDF.

Ogni pagina mostra `PASS` quando tutte le verifiche sono completate correttamente.
